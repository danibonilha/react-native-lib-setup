enum RelativePlatormPath {
  ios = "./ios",
  android = "./android",
}

enum Libraries {
  navigation = 'react-navigation'
}

enum Cmd {
  yarn = 'yarn',
  add = 'add',
  npx = 'npx',
  pod = 'pod-install',
  ios = 'ios'
}

export {
  RelativePlatormPath,
  Libraries,
  Cmd
}